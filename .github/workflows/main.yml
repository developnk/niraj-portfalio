name: Node.js CI/CD

on:
    push:
        branches:
            - main

jobs:
    build:
        runs-on: ubuntu-latest

        steps:
            - name: Checkout Repository
              uses: actions/checkout@v3

            - name: Set up Node.js
              uses: actions/setup-node@v3
              with:
                  node-version: '20.x'

            - name: npm install, build
              run: |
                  npm install
                  CI="" npm run build
            - name: List files in build directory
              run: ls -al build

            - name: Upload artifact for deployment job
              uses: actions/upload-artifact@v2
              with:
                  name: node-app
                  path: build

    deploy:
        runs-on: ubuntu-latest
        needs: build

        steps:
            - name: Download artifact from build job
              uses: actions/download-artifact@v2
              with:
                  name: node-app

            - name: List downloaded files
              run: ls -al

            - name: Copy To Server
              uses: appleboy/scp-action@master
              with:
                  host: ${{ vars.SANDBOX_SERVER_HOST_92 }}
                  username: ${{ vars.SANDBOX_USER_92 }}
                  password: ${{ vars.SANDBOX_PWD_92 }}
                  source: '.'
                  target: /var/www/CareerGeniusAI/nirajportfalio/${{ github.run_id }}

            - name: Run Scripts on server
              uses: appleboy/ssh-action@v1.0.0
              with:
                  host: ${{ vars.SANDBOX_SERVER_HOST_92 }}
                  username: ${{ vars.SANDBOX_USER_92 }}
                  password: ${{ vars.SANDBOX_PWD_92 }}
                  script: |
                      echo ${{ github.run_number }} 
                      mkdir -p /var/www/CareerGeniusAI/nirajportfalio/${{ github.run_number }}.${{ github.run_attempt }}
                      mv -f /var/www/CareerGeniusAI/nirajportfalio/html/. /var/www/CareerGeniusAI/nirajportfalio/${{ github.run_number }}.${{ github.run_attempt }}/
                      cp -rf /var/www/CareerGeniusAI/nirajportfalio/${{ github.run_id }}/. /var/www/CareerGeniusAI/nirajportfalio/html/
                      sudo service apache2 restart
                      rm -r /var/www/CareerGeniusAI/nirajportfalio/${{ github.run_id }}
            
